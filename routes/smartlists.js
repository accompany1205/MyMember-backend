const express = require('express');
const router = express.Router();
const {  } = require("../controllers/smartlists");
const { requireSignin, isAuth, verifySchool } = require("../controllers/auth");



module.exports = router;